const ping = require('./ping');
const login = require('./login');
const getUser = require('./getUser');
const getSpider = require('./getSpider');
const signup = require('./signup');

module.exports = {
  ping,
  login,
  getUser,
  getSpider,
  signup,
};
